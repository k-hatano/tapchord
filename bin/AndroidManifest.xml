<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jp.nita.tapchord"
    android:versionCode="7"
    android:versionName="2.0α" android:installLocation="auto">

    <uses-sdk
        android:minSdkVersion="23"
        android:targetSdkVersion="23" />
    <uses-permission android:name="android.permission.VIBRATE"/>

    <uses-feature android:name="android.software.midi" android:required="true"/>

    <application
        android:allowBackup="true"
        android:name="jp.nita.tapchord.TapChordApplication"
        android:icon="@drawable/tapchord"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name="jp.nita.tapchord.MainActivity"
            android:label="@string/app_name" android:screenOrientation="landscape" android:theme="@android:style/Theme.Light.NoTitleBar.Fullscreen" android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="jp.nita.tapchord.SettingsActivity" android:theme="@android:style/Theme.Light" android:screenOrientation="landscape" android:label="@string/tapchord_settings" android:launchMode="singleTask"></activity>
    </application>

</manifest>
